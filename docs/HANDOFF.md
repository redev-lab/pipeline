# 인수인계 (다음 세션)

**다음 = `features.py` (graph.md §4 분해 따름).**

핵심: `_buildings_as_of(t)` 공통 헬퍼로 건물 파생 피처(노후도·호수밀도)를 전부 시점
정합(보강1, R1 누수 차단 — 노후도만 시점필터하면 호수밀도에 t 이후 신축 빌라가 샌다).

수검(규칙9) = ① 같은 PNU 두 t 행의 피처가 *실제로 다른지* ② 결측/범위 ③ R5 두-행
케이스. **베이스라인(XGBoost)도 동일 v1 피처(R9 공정성)** 명심.

---

## Phase 2 진행
- ✅ `graph/build.py` — 필지 인접그래프(R10 배치, 수정1 노드제외 측정기반, reconcile). 수검 통과.
- ⏳ `graph/features.py` — 노드 피처(노후도 시점·면적·형상·호수밀도·접도), 보강1 as-of-t.

## v1 피처 (graph.md §3): 노후도(시점)·면적·형상·호수밀도·접도(지목='도' 인접).
데이터 대기(v1.1): 용도지역·공시지가·역거리·배제레이어.

## 미결 (학습 전, Phase 3): 완공 9구역 drop vs v1.1 말소대장 부활(labels §13).
