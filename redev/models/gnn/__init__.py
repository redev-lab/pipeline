"""models.gnn — 심장1: GraphSAGE/GAT 노드 분류 (Phase 3).

역할: 인접 그래프 위에서 필지를 '재개발 후보 / 아님'으로 분류하고, 연결된
positive component를 후보 폴리곤으로 묶는다. inductive 설계여야 학습 안 한
구에도 추론 가능(scale-ready).

(model.py / train.py / infer.py — 아직 비어 있음.)
"""
